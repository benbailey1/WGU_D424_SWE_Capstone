@page "/"
@using StudentTermTracker.Services
@inject IAuthenticationService AuthService
@inject NavigationManager NavigationManager

@code {
    protected override async Task OnInitializedAsync()
    {
        await AuthService.FetchUserAccountSession();
        
        if (AuthService.UserAccountSession == null)
        {
            NavigationManager.NavigateTo("/login");
        }
        else
        {
            NavigationManager.NavigateTo("/terms");
        }
    }
}

<h1>Redirecting...</h1>

Welcome to your portal for managing Terms, Courses, and Assessments.
